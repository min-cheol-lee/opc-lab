{
  "name": "litopc-frontend",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:clean": "powershell -NoProfile -Command \"if (Test-Path .next) { try { Remove-Item .next -Recurse -Force -ErrorAction Stop } catch { Write-Host 'Skipping .next cleanup because it is in use.' } }\"; next dev",
    "build": "next build",
    "report:fonts": "node scripts/report-font-usage.mjs",
    "report:fonts:build": "powershell -NoProfile -Command \"$env:NEXT_DIST_DIR='.next-font-report'; next build; if ($LASTEXITCODE -eq 0) { node scripts/report-font-usage.mjs }\""
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "25.2.3",
    "@types/react": "19.2.14",
    "typescript": "5.9.3"
  }
}
